interface:
  display_name: "Generate Museum JSON"
  short_description: "Create, review, and validate museum JSON configurations that this project can load directly"
  default_prompt: "Use $generate-museum-json to create and validate a museum JSON configuration for this project."
